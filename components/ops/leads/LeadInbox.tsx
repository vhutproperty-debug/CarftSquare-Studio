'use client';

import DemandWorkspace from '@/components/ops/demand/DemandWorkspace';

/** @deprecated Use DemandWorkspace — preserved for import compatibility */
export default function LeadInbox() {
  return <DemandWorkspace />;
}
